import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    artists: []
}

export const artistsSlice = createSlice({
    name: 'artists',
    initialState,
    reducers: {
        
    }
})